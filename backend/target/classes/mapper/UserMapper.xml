<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.scheduling.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.school.scheduling.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="role" property="role"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="findTeachers" resultMap="BaseResultMap">
        SELECT id, username, real_name, role, phone, email, create_time, update_time 
        FROM user WHERE role = 'TEACHER' ORDER BY id
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT id, username, real_name, role, phone, email, create_time, update_time 
        FROM user ORDER BY id
    </select>

    <insert id="insert" parameterType="com.school.scheduling.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, real_name, role, phone, email)
        VALUES (#{username}, #{password}, #{realName}, #{role}, #{phone}, #{email})
    </insert>

    <update id="update" parameterType="com.school.scheduling.entity.User">
        UPDATE user
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="password != null">password = #{password},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id} AND role = 'TEACHER'
    </delete>
</mapper>
